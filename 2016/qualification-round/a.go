package main

import (
	"bufio"
	"math"
	"fmt"
	"os"
)

func main() {
	var list []string
	var num int
	fmt.Scanf("%d", &num)
	fmt.Print(num)

	sc := bufio.NewScanner(os.Stdin)

	for i := 0; i < num; i++ {
		sc.Scan()
		j := sc.Text()
		do {
			a := j
		} while
	}
}
